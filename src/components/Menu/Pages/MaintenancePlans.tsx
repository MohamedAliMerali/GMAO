import { User } from "../../../Data/users";

interface Props {
  user: User;
}

const MaintenancePlans = ({ user }: Props) => {
  return <div>MaintenancePlans</div>;
};

export default MaintenancePlans;
