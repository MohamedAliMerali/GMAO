import { User } from "../../../Data/users";

interface Props {
  user: User;
}

const MachineDocuments = ({ user }: Props) => {
  return <div>MachineDocuments</div>;
};

export default MachineDocuments;
